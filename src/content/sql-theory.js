// Extensive per-topic theory content for the SQL trainer (SQLite dialect).
const TOPIC_THEORY = {
  create_table: {
    title: 'CREATE TABLE — создание таблиц',
    md: `# CREATE TABLE — создание таблиц

## Базовый синтаксис

\`\`\`sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  price REAL NOT NULL
);
\`\`\`

Таблица описывается как набор столбцов вида \`имя_столбца тип_данных [ограничения]\`, разделённых запятыми.

## Типы данных в SQLite (type affinity)

В отличие от многих СУБД, SQLite использует **динамическую типизацию** со «сродством типов» (type affinity). Указанный тип столбца — это скорее подсказка, чем жёсткое правило. Основные аффинности:

| Тип в CREATE TABLE | Аффинность | Что хранит |
|---|---|---|
| \`INTEGER\` | INTEGER | целые числа |
| \`REAL\`, \`FLOAT\`, \`DOUBLE\` | REAL | числа с плавающей точкой |
| \`TEXT\`, \`VARCHAR(n)\`, \`CHAR(n)\` | TEXT | строки |
| \`BLOB\` | BLOB | бинарные данные "как есть" |
| без указания типа | BLOB | любое значение |
| \`NUMERIC\`, \`DECIMAL\`, \`BOOLEAN\`, \`DATE\` | NUMERIC | пытается хранить как число, иначе как текст |

На практике для учебных задач достаточно \`INTEGER\`, \`TEXT\` и \`REAL\`.

## Ограничения столбцов (constraints)

- **\`PRIMARY KEY\`** — первичный ключ, уникальный идентификатор строки. Столбец \`INTEGER PRIMARY KEY\` в SQLite становится псевдонимом встроенного \`rowid\` — самый быстрый способ обращения к строке.
- **\`NOT NULL\`** — запрещает пустое значение (\`NULL\`) в столбце. Без этого ограничения столбец по умолчанию допускает \`NULL\`.
- **\`UNIQUE\`** — все значения в столбце должны быть уникальны (но \`NULL\` может повторяться сколько угодно раз — по стандарту SQL \`NULL\` не считается равным другому \`NULL\`).
- **\`DEFAULT значение\`** — значение, которое подставляется, если при \`INSERT\` столбец не указан явно.
- **\`CHECK (условие)\`** — произвольное условие, которое должно выполняться для каждой строки, например \`CHECK (price >= 0)\`.
- **\`REFERENCES другая_таблица(столбец)\`** — внешний ключ (foreign key), связывающий строку с записью другой таблицы (в SQLite проверка внешних ключей включается отдельно командой \`PRAGMA foreign_keys = ON;\`).

Пример таблицы с несколькими ограничениями:

\`\`\`sql
CREATE TABLE orders (
  id INTEGER PRIMARY KEY,
  customer_id INTEGER NOT NULL REFERENCES customers(id),
  status TEXT NOT NULL DEFAULT 'new' CHECK (status IN ('new', 'paid', 'shipped')),
  total REAL NOT NULL CHECK (total >= 0)
);
\`\`\`

## AUTOINCREMENT

\`INTEGER PRIMARY KEY\` уже автоматически генерирует возрастающие идентификаторы, если не указывать значение при вставке. Ключевое слово \`AUTOINCREMENT\` (\`INTEGER PRIMARY KEY AUTOINCREMENT\`) дополнительно гарантирует, что уже использованные id никогда не будут переиспользованы повторно — это чуть медленнее и нужно редко.

## IF NOT EXISTS

Чтобы запрос не завершался ошибкой при повторном выполнении, если таблица уже существует:

\`\`\`sql
CREATE TABLE IF NOT EXISTS products (...);
\`\`\`

## Составной первичный ключ

Если уникальность строки определяется сразу несколькими столбцами:

\`\`\`sql
CREATE TABLE enrollments (
  student_id INTEGER NOT NULL,
  course_id INTEGER NOT NULL,
  PRIMARY KEY (student_id, course_id)
);
\`\`\`

## Изменение и удаление структуры

- \`ALTER TABLE имя ADD COLUMN ...\` — добавить новый столбец в существующую таблицу.
- \`ALTER TABLE имя RENAME TO новое_имя\` — переименовать таблицу.
- \`DROP TABLE имя\` — удалить таблицу целиком (отдельная тема ниже).

## Частые ошибки

- Забыть запятую между столбцами или лишняя запятая после последнего столбца.
- Указать два столбца с \`PRIMARY KEY\` без явного составного ключа.
- Забыть \`NOT NULL\`, из-за чего в "обязательный" на бумаге столбец можно записать \`NULL\`.
- Опечатка в названии типа — SQLite не выдаст ошибку (аффинность просто станет BLOB), но столбец будет вести себя не так, как ожидалось.
`,
  },

  insert: {
    title: 'INSERT INTO — добавление данных',
    md: `# INSERT INTO — добавление данных

## Базовый синтаксис

\`\`\`sql
INSERT INTO products (id, name, price) VALUES (1, 'Ноутбук', 55000);
\`\`\`

Формат: \`INSERT INTO таблица (список_столбцов) VALUES (список_значений)\`. Порядок значений должен строго соответствовать порядку перечисленных столбцов.

## Вставка без списка столбцов

Если список столбцов не указан, значения должны идти строго в том порядке, в котором столбцы объявлены в \`CREATE TABLE\`:

\`\`\`sql
INSERT INTO products VALUES (1, 'Ноутбук', 'Электроника', 55000, 10);
\`\`\`

Этот способ короче, но хрупкий: если структура таблицы изменится (добавится столбец), запрос сломается или вставит данные не туда. На практике почти всегда явно перечисляют столбцы.

## Вставка нескольких строк одним запросом

\`\`\`sql
INSERT INTO products (id, name, price) VALUES
  (1, 'Ноутбук', 55000),
  (2, 'Мышь', 1200),
  (3, 'Клавиатура', 2500);
\`\`\`

Это эффективнее, чем три отдельных \`INSERT\`: один проход, одна транзакция.

## Пропущенные столбцы и DEFAULT

Если столбец не перечислен в списке колонок, он получает значение по умолчанию:

- значение из \`DEFAULT ...\`, если оно задано в \`CREATE TABLE\`;
- иначе \`NULL\`, если столбец это допускает;
- если столбец \`NOT NULL\` без \`DEFAULT\` — вставка завершится ошибкой \`NOT NULL constraint failed\`.

\`\`\`sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  quantity INTEGER NOT NULL DEFAULT 0
);

INSERT INTO products (id, name) VALUES (1, 'Ноутбук'); -- quantity станет 0
\`\`\`

## INSERT ... SELECT

Можно вставить в таблицу результат другого запроса — например, скопировать отфильтрованные данные из одной таблицы в другую:

\`\`\`sql
INSERT INTO archived_products (id, name, price)
SELECT id, name, price FROM products WHERE quantity = 0;
\`\`\`

## DEFAULT VALUES

Если для всех столбцов заданы значения по умолчанию (или разрешён \`NULL\`), можно вставить полностью "пустую" строку:

\`\`\`sql
INSERT INTO logs DEFAULT VALUES;
\`\`\`

## Обработка конфликтов: OR IGNORE / OR REPLACE

Если вставка нарушает ограничение уникальности (\`PRIMARY KEY\` или \`UNIQUE\`), по умолчанию запрос завершится ошибкой. Это поведение можно изменить:

\`\`\`sql
INSERT OR IGNORE INTO products (id, name, price) VALUES (1, 'Дубликат', 100); -- строка просто не будет вставлена, ошибки не будет
INSERT OR REPLACE INTO products (id, name, price) VALUES (1, 'Заменённый товар', 100); -- существующая строка с id=1 будет удалена и вставлена заново
\`\`\`

## RETURNING (современный SQLite)

Начиная с SQLite 3.35, можно сразу получить вставленные данные без отдельного \`SELECT\`:

\`\`\`sql
INSERT INTO products (name, price) VALUES ('Монитор', 15000) RETURNING id;
\`\`\`

## Частые ошибки

- \`NOT NULL constraint failed: products.name\` — не указано обязательное значение и нет \`DEFAULT\`.
- \`UNIQUE constraint failed: products.id\` — попытка вставить строку с уже существующим значением первичного/уникального ключа.
- Несовпадение количества столбцов и количества значений в \`VALUES\`.
- Кавычки: строковые значения обязательно в одинарных кавычках \`'текст'\`, двойные кавычки в SQLite означают идентификатор (имя столбца/таблицы), а не строку.
`,
  },

  select: {
    title: 'SELECT — выборка данных',
    md: `# SELECT — выборка данных

## Полная анатомия запроса

\`\`\`sql
SELECT [DISTINCT] столбцы
FROM таблица
[WHERE условие]
[GROUP BY столбцы]
[HAVING условие_на_группы]
[ORDER BY столбцы [ASC|DESC]]
[LIMIT n [OFFSET m]];
\`\`\`

Порядок написания секций строго фиксирован, но выполняются они логически в другом порядке: сначала \`FROM\`, затем \`WHERE\`, затем \`GROUP BY\`/\`HAVING\`, затем вычисляются столбцы \`SELECT\`, затем \`ORDER BY\`, и в конце \`LIMIT\`.

## Выбор столбцов

\`\`\`sql
SELECT * FROM products;                 -- все столбцы
SELECT name, price FROM products;       -- только нужные столбцы
SELECT name AS product_name FROM products; -- псевдоним (алиас) столбца
SELECT price * 1.2 AS price_with_tax FROM products; -- вычисляемый столбец
\`\`\`

\`AS\` можно опускать (\`SELECT name product_name FROM products\` тоже работает), но с \`AS\` запрос читается понятнее.

## DISTINCT

Убирает повторяющиеся строки из результата:

\`\`\`sql
SELECT DISTINCT category FROM products;
\`\`\`

## Сортировка — ORDER BY

\`\`\`sql
SELECT * FROM products ORDER BY price ASC;   -- по возрастанию (по умолчанию)
SELECT * FROM products ORDER BY price DESC;  -- по убыванию
SELECT * FROM products ORDER BY category ASC, price DESC; -- сначала по категории, внутри — по цене
\`\`\`

## Ограничение количества строк — LIMIT / OFFSET

\`\`\`sql
SELECT * FROM products ORDER BY price DESC LIMIT 3;         -- топ-3 самых дорогих
SELECT * FROM products ORDER BY id LIMIT 5 OFFSET 10;       -- строки с 11-й по 15-ю (постраничная выборка)
\`\`\`

## Агрегатные функции и GROUP BY

Агрегатные функции сворачивают набор строк в одно значение: \`COUNT()\`, \`SUM()\`, \`AVG()\`, \`MIN()\`, \`MAX()\`.

\`\`\`sql
SELECT COUNT(*) FROM products;                          -- сколько всего товаров
SELECT category, COUNT(*) AS cnt FROM products GROUP BY category;   -- сколько товаров в каждой категории
SELECT category, AVG(price) AS avg_price FROM products GROUP BY category;
\`\`\`

\`HAVING\` — это «WHERE для групп», фильтрует уже после группировки (\`WHERE\` фильтрует строки ДО группировки и не может использовать агрегатные функции):

\`\`\`sql
SELECT category, COUNT(*) AS cnt
FROM products
GROUP BY category
HAVING COUNT(*) > 2;
\`\`\`

## Подзапросы (кратко)

Запрос может использовать результат другого запроса:

\`\`\`sql
SELECT * FROM products WHERE price > (SELECT AVG(price) FROM products);
\`\`\`

## JOIN (кратко, для общего понимания)

Когда данные лежат в нескольких связанных таблицах, их можно объединить:

\`\`\`sql
SELECT orders.id, customers.name
FROM orders
JOIN customers ON orders.customer_id = customers.id;
\`\`\`

Подробно JOIN разбирается в отдельной, более продвинутой теме — здесь достаточно знать, что такая возможность существует.

## Частые ошибки

- Использование алиаса столбца (заданного через \`AS\`) в том же \`SELECT\`, где он определён, внутри \`WHERE\` — это не работает, \`WHERE\` выполняется раньше, чем вычисляются алиасы. Нужно повторить выражение или использовать подзапрос.
- Попытка выбрать столбец, не входящий ни в \`GROUP BY\`, ни в агрегатную функцию.
- Путаница между \`WHERE\` (фильтр строк) и \`HAVING\` (фильтр групп).
`,
  },

  where: {
    title: 'WHERE — фильтрация данных',
    md: `# WHERE — фильтрация данных

\`WHERE\` отбирает только те строки, для которых указанное условие истинно (\`TRUE\`). Работает не только в \`SELECT\`, но и в \`UPDATE\`/\`DELETE\`.

## Операторы сравнения

| Оператор | Значение |
|---|---|
| \`=\` | равно |
| \`!=\` или \`<>\` | не равно |
| \`>\`, \`<\` | больше, меньше |
| \`>=\`, \`<=\` | больше или равно, меньше или равно |

\`\`\`sql
SELECT * FROM products WHERE price > 3000;
SELECT * FROM products WHERE category = 'Мебель';
\`\`\`

## Логические операторы: AND, OR, NOT

\`\`\`sql
SELECT * FROM products WHERE category = 'Мебель' AND price < 5000;
SELECT * FROM products WHERE category = 'Книги' OR category = 'Канцтовары';
SELECT * FROM products WHERE NOT category = 'Мебель';
\`\`\`

**Важно про приоритет операторов**: \`AND\` имеет более высокий приоритет, чем \`OR\`, — так же, как умножение важнее сложения в арифметике. Поэтому:

\`\`\`sql
-- Это НЕ значит "(Мебель или Книги) и дёшево" — это значит "Мебель ИЛИ (Книги и дёшево)"
WHERE category = 'Мебель' OR category = 'Книги' AND price < 1000;

-- Чтобы получить правильный смысл, используйте скобки:
WHERE (category = 'Мебель' OR category = 'Книги') AND price < 1000;
\`\`\`

Правило: **если сомневаетесь — ставьте скобки явно**.

## BETWEEN — диапазон значений

\`\`\`sql
SELECT * FROM products WHERE price BETWEEN 1000 AND 5000; -- включительно, эквивалент price >= 1000 AND price <= 5000
\`\`\`

## IN — проверка на вхождение в список

\`\`\`sql
SELECT * FROM products WHERE category IN ('Мебель', 'Книги', 'Канцтовары');
-- эквивалент: category = 'Мебель' OR category = 'Книги' OR category = 'Канцтовары'
\`\`\`

## LIKE и GLOB — поиск по шаблону

\`LIKE\` использует два спецсимвола:
- \`%\` — любое количество любых символов (включая ноль);
- \`_\` — ровно один любой символ.

\`\`\`sql
SELECT * FROM products WHERE name LIKE 'С%';    -- начинается на "С"
SELECT * FROM products WHERE name LIKE '%ка';   -- заканчивается на "ка"
SELECT * FROM products WHERE name LIKE '%ноут%'; -- содержит "ноут" где угодно
SELECT * FROM products WHERE name LIKE '_тол';  -- ровно один символ перед "тол" (например "стол")
\`\`\`

По умолчанию \`LIKE\` в SQLite регистронезависим для латиницы и **регистрозависим для кириллицы**. Альтернатива — \`GLOB\`, который использует шаблоны в стиле Unix (\`*\`, \`?\`) и всегда регистрозависим.

## NULL — особый случай

\`NULL\` означает «значение неизвестно/отсутствует» и не равно ничему, даже другому \`NULL\`. Поэтому:

\`\`\`sql
WHERE price = NULL      -- НЕПРАВИЛЬНО, никогда не сработает
WHERE price IS NULL     -- правильно
WHERE price IS NOT NULL -- правильно
\`\`\`

## Частые ошибки

- Забытые скобки при смешивании \`AND\`/\`OR\` — см. выше, самая частая логическая ошибка новичков.
- Сравнение с \`NULL\` через \`=\` вместо \`IS NULL\`.
- Опечатка в названии столбца или значении (регистр текста в кириллице важен для точного сравнения \`=\`).
- Забыть \`WHERE\` в \`UPDATE\`/\`DELETE\` — тогда операция применится **ко всем строкам таблицы**.
`,
  },

  update: {
    title: 'UPDATE — изменение данных',
    md: `# UPDATE — изменение данных

## Базовый синтаксис

\`\`\`sql
UPDATE таблица
SET столбец1 = значение1, столбец2 = значение2, ...
WHERE условие;
\`\`\`

\`\`\`sql
UPDATE products SET price = 60000 WHERE id = 1;
\`\`\`

## Изменение нескольких столбцов сразу

\`\`\`sql
UPDATE products
SET price = 60000, quantity = 5
WHERE id = 1;
\`\`\`

## Изменение нескольких строк по условию

\`WHERE\` в \`UPDATE\` работает так же, как в \`SELECT\` — если условию соответствуют несколько строк, изменятся все они:

\`\`\`sql
UPDATE products SET price = 2500 WHERE category = 'Мебель';
\`\`\`

## Выражения на основе текущего значения

Новое значение может зависеть от старого значения того же столбца — SQLite сначала считает выражение справа, используя старые значения строки, потом присваивает результат:

\`\`\`sql
UPDATE products SET price = price * 1.1 WHERE category = 'Электроника'; -- подорожание на 10%
UPDATE products SET quantity = quantity - 1 WHERE id = 3;               -- списать одну единицу товара
\`\`\`

## ⚠️ UPDATE без WHERE

Если не указать \`WHERE\`, обновятся **абсолютно все строки таблицы**:

\`\`\`sql
UPDATE products SET price = 0; -- обнулит цену у ВСЕХ товаров — почти всегда ошибка
\`\`\`

Перед выполнением \`UPDATE\` в реальной работе полезно сначала выполнить тот же \`WHERE\` в виде \`SELECT\`, чтобы увидеть, какие именно строки будут затронуты.

## UPDATE с подзапросом

\`\`\`sql
UPDATE products
SET price = price * 0.9
WHERE category = (SELECT category FROM categories WHERE name = 'Устаревшее');
\`\`\`

## RETURNING (современный SQLite)

\`\`\`sql
UPDATE products SET price = 60000 WHERE id = 1 RETURNING id, price;
\`\`\`

## Частые ошибки

- Забытый \`WHERE\` — самая опасная и частая ошибка с \`UPDATE\`.
- Перепутать порядок: \`SET цена = 60000\` — слева всегда имя столбца, справа — новое значение.
- Опечатка в названии столбца в \`SET\` — SQLite создаст ошибку "no such column", запрос не выполнится (это, скорее, спасает от более серьёзных ошибок).
- Ожидание, что \`UPDATE\` вернёт изменённые строки без \`RETURNING\` — по умолчанию \`UPDATE\` ничего не выводит, только сообщает число изменённых строк.
`,
  },

  delete: {
    title: 'DELETE — удаление данных',
    md: `# DELETE — удаление данных (строк)

## Базовый синтаксис

\`\`\`sql
DELETE FROM таблица WHERE условие;
\`\`\`

\`\`\`sql
DELETE FROM products WHERE id = 6;
\`\`\`

Важно: \`DELETE\` удаляет **строки** (записи), а не саму таблицу и не столбцы. Структура таблицы остаётся нетронутой.

## Удаление нескольких строк по условию

\`\`\`sql
DELETE FROM products WHERE category = 'Мебель';
DELETE FROM products WHERE quantity = 0;
DELETE FROM products WHERE price < 100 OR quantity > 500;
\`\`\`

## ⚠️ DELETE без WHERE — удаление всех строк

\`\`\`sql
DELETE FROM products; -- удалит ВСЕ строки, таблица останется пустой, но не исчезнет
\`\`\`

Это отличается от \`DROP TABLE products\`, которая уничтожает саму таблицу вместе со структурой (см. следующую тему). \`DELETE FROM таблица;\` без \`WHERE\` — законный способ полностью очистить таблицу, сохранив её структуру для дальнейшего использования.

## DELETE и внешние ключи

Если на удаляемые строки ссылаются другие таблицы через \`FOREIGN KEY\` (и включена проверка \`PRAGMA foreign_keys = ON\`), удаление может быть заблокировано ошибкой \`FOREIGN KEY constraint failed\`, либо (если объявлено \`ON DELETE CASCADE\`) автоматически повлечёт удаление связанных строк в других таблицах.

## DELETE с подзапросом

\`\`\`sql
DELETE FROM products
WHERE category IN (SELECT name FROM categories WHERE discontinued = 1);
\`\`\`

## RETURNING (современный SQLite)

Чтобы увидеть, что именно было удалено:

\`\`\`sql
DELETE FROM products WHERE quantity = 0 RETURNING id, name;
\`\`\`

## Частые ошибки

- Забытый \`WHERE\` — удаляются все строки таблицы вместо нужных.
- Путаница \`DELETE\` (удаляет строки) и \`DROP TABLE\` (удаляет саму таблицу) — это разные по масштабу операции.
- Надежда на "отмену" после выполнения — без обёртывания в транзакцию с явным \`ROLLBACK\` удалённые данные не восстановить.
`,
  },

  drop: {
    title: 'DROP TABLE — удаление таблицы',
    md: `# DROP TABLE — удаление таблицы целиком

## Базовый синтаксис

\`\`\`sql
DROP TABLE products;
\`\`\`

Полностью удаляет таблицу: и структуру (описание столбцов, ограничений), и все данные, и все связанные с ней индексы и триггеры. После выполнения обращение к этой таблице (\`SELECT\`, \`INSERT\` и т.д.) вернёт ошибку "no such table".

## DROP TABLE vs DELETE FROM

Это принципиально разные по масштабу операции:

| | \`DELETE FROM products;\` | \`DROP TABLE products;\` |
|---|---|---|
| Что удаляется | все строки (данные) | вся таблица целиком |
| Структура таблицы | сохраняется | исчезает |
| Можно продолжать вставлять данные | да, таблица пустая, но существует | нет, нужно создавать заново через \`CREATE TABLE\` |

## IF EXISTS

Чтобы не получить ошибку, если таблицы уже нет (например, при повторном выполнении скрипта):

\`\`\`sql
DROP TABLE IF EXISTS old_orders;
\`\`\`

## Необратимость операции

\`DROP TABLE\` — одна из самых опасных команд в SQL: **отменить её нельзя** (если не выполнялась внутри явной транзакции, которую ещё не зафиксировали, либо нет резервной копии базы данных). Перед выполнением на реальных данных стоит:

1. Убедиться, что вы работаете с правильной базой данных / правильным окружением (не production по ошибке).
2. Сделать резервную копию, если данные важны.
3. Проверить, что имя таблицы указано верно (опечатка в имени с \`IF EXISTS\` просто тихо ничего не сделает и не даст об этом знать).

## Связанные объекты

Вместе с таблицей автоматически удаляются:
- все **индексы**, созданные на этой таблице;
- все **триггеры**, привязанные к этой таблице;
- запись о таблице в служебном каталоге \`sqlite_master\`.

Представления (\`VIEW\`), которые ссылались на удалённую таблицу, не удаляются автоматически, но перестают работать при обращении к ним.

## Проверка существования таблицы

Убедиться, что таблица действительно удалена, можно через служебную таблицу \`sqlite_master\`:

\`\`\`sql
SELECT name FROM sqlite_master WHERE type = 'table' AND name = 'products';
-- если DROP TABLE сработал, этот запрос вернёт пустой результат
\`\`\`

## Частые ошибки

- Опечатка в имени таблицы вместе с \`IF EXISTS\` — команда "успешно" ничего не удаляет, и это легко не заметить.
- Путаница с \`DELETE FROM\` — см. таблицу сравнения выше.
- Выполнение на таблице, на которую ссылаются другие таблицы через \`FOREIGN KEY\`, — в зависимости от настроек это может завершиться ошибкой или оставить "битые" ссылки.
`,
  },
};

module.exports = { TOPIC_THEORY };
